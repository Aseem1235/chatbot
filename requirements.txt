# Core Django + API
Django==5.2.2
djangorestframework==3.16.0
djangorestframework_simplejwt==5.5.0
django-cors-headers==4.7.0

# OpenAI
openai==1.92.2

# Env config (if you're using .env files for secrets)
python-dotenv==1.1.1

# MySQL (if you're using MySQL)
mysqlclient==2.2.7

# Optional: If using PyJWT manually (not needed if only using SimpleJWT)
PyJWT==2.9.0
